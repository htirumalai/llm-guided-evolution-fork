Todos:
- Verify results are properly ingested from the results file (Analyzed: Reason was the timeout was forcing the specific genes to not be updated to the results)
- Verify that parameter count is accurate
    - Look at how the metric is computed
    - Load in some of the hashes network's and verify that there are no changes to the networks that would affect parameter count
- Compare some models generated by llm, verify that changes made are in code that is exercised in evaluation, if not, we may need to work on prompts to be more specific
- Investigate train / test performance on single models
- Look into changes to speed up evaluation process (e.g. early stopping based on training / validation scores, patience, etc) (Early Stopping implemented)
- Scale up on icehammer if confident in code (larger pops)
- Investigate random seeds, take the seed